<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AdonisJS Crash Course </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" integrity="sha512-+4zCK9k+qNFUR5X+cKL9EIR+ZOhtIloNl9GIKS57V1MyNsYpYcUrUeQc9vNfzsWfV28IaLL3i96P9sdNyeRssA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
    <nav class="w-full bg-white shadow-md border flex justify-center px-4 py-2">
        <div class="w-1/2 flex justify-between">
            <h1 class="text-xl find-semibold">
                <a href="/">Adonisgram</a>
            </h1>
            @if(auth.isAuthenticated && auth.user.email_verified_at === 0)
                <form action="/verify-email" method="post">
                    <input type="submit" value="resend" class="px-2 py-1 rounded">
                </form>
            @endif
            <div>
                @if(auth.isAuthenticated)
                <a href="/{{auth.user.username}}">
                    <img src="{{ auth.user.avatar }}" class="h-8 w-8 rounded-full border" alt="profile">
                </a>
                @endif
            </div>
        </div>
    </nav>
    <div class="bg-gray-100 min-h-screen flex justify-center">
        <div class="w-full md:w-1/2">
            @section('content')
            @endsection
        </div>
    </div>
</body>
</html>